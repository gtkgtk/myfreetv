package org.rom.myfreetv.view;

import javax.swing.JDialog;

class EmissionDetailsDialog extends JDialog {

    // private Frame owner;
    // private EmissionDetailsPanel details;
    //
    // EmissionDetailsDialog(Frame owner, Emission emission) {
    // super(owner,true);
    // this.owner = owner;
    //        
    // Point p = owner.getLocation();
    // Dimension d = owner.getSize();
    // int x1 = (int) p.getX();
    // int y1 = (int) p.getY();
    // int x2 = x1 + (int) d.getWidth();
    // int y2 = y1 + (int) d.getHeight();
    //        
    // details = new EmissionDetailsPanel();
    // details.set(emission);
    // getContentPane().add(details);
    // // setContentPane(details);
    //        
    // pack();
    //
    // int posX = (x1 + x2 - (int) getSize().getWidth()) / 2;
    // int posY = (y1 + y2 - (int) getSize().getHeight()) / 2;
    //
    // setLocation(posX, posY);
    //        
    // setVisible(true);
    // }

    // public void showAt(Component invoker, Emission emission, int x, int y) {
    // int width = 200;
    // int height = 100;
    // details.set(emission);
    // if(invoker != null) {
    // Point invokerOrigin = invoker.getLocationOnScreen();
    // x += invokerOrigin.x;
    // y += invokerOrigin.y;
    // }
    //
    // this.setBounds(x + 15, y + 15, width + 7, height + 7);
    // this.setVisible(true);
    // this.toFront();
    // }

    // private static Frame getFrame(Component c) {
    // Component w = c;
    // while(!(w instanceof Frame) && (w != null)) {
    // w = w.getParent();
    // }
    // return (Frame) w;
    // }

}
